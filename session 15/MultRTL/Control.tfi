


// Instantiate the module
Control instance_name (
    .LSB(LSB), 
    .Clk(Clk), 
    .Rst(Rst), 
    .Load(Load), 
    .Add(Add), 
    .Shift(Shift), 
    .Stop(Stop), 
    .Start(Start)
    );


